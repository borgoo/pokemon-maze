<Window x:Class="Pokémon.Maze.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="BFS maze"
        MinWidth="640"
        MinHeight="480">
    <Border Margin="8"
            BorderBrush="#444"
            BorderThickness="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
        <Viewbox Stretch="Uniform" MaxWidth="1280" MaxHeight="928">
            <Grid Width="640" Height="464" SnapsToDevicePixels="True">
                <Image x:Name="MazeBackground"
                       Width="640"
                       Height="464"
                       Stretch="Fill"
                       RenderOptions.BitmapScalingMode="NearestNeighbor" />
                <Canvas x:Name="OverlayCanvas" Width="640" Height="464" IsHitTestVisible="False" />
                <Canvas x:Name="SpriteCanvas" Width="640" Height="464" IsHitTestVisible="False">
                    <Image x:Name="SpriteImage"
                           Width="16"
                           Height="16"
                           RenderOptions.BitmapScalingMode="NearestNeighbor"
                           Visibility="Collapsed" />
                </Canvas>
            </Grid>
        </Viewbox>
    </Border>
</Window>
